package com.example;

import static org.junit.Assert.assertEquals;

import com.Register;
import org.junit.Assert;
import org.junit.Test;

import java.util.HashMap;

public class RegisterTest {

  @Test
  public void RegisterTest() {
    //HashMap<String, Integer> createAccount = new HashMap<>();

    /*Register createAccount = new Register(accounts);

    boolean result = createAccount.containsValue("2356");

    Assert.assertEquals("ID guardado", result, null);*/
  }
}
